apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "harbor-vulnerabilities-exporter.fullname" . }}-test-connection"
  labels:
    {{- include "harbor-vulnerabilities-exporter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "harbor-vulnerabilities-exporter.fullname" . }}:{{ .Values.port }}']
  restartPolicy: Never
